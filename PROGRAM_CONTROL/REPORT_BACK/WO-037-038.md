# REPORT BACK: WO-037 / WO-038 — Growth Primitives Scaffolding (Phase 3)

## Branch

`copilot/wo-037-create-attribution-engine`

## HEAD Commit

See latest commit on branch.

## Files Changed (`git diff --stat`)

```
.github/required-files.txt                                    |   3 +
infra/postgres/init-ledger.sql                                | 239 +++++++++++++++++++
services/core-api/src/app.module.ts                           |   5 +-
services/core-api/src/growth/referral-reward.service.ts       | NEW
services/core-api/src/growth/guarded-notification.service.ts  | NEW
services/core-api/src/growth/growth.module.ts                 | NEW
PROGRAM_CONTROL/REPORT_BACK/WO-037-038.md                     | NEW
```

## Commands Run & Outputs

### 1. SQL Schema Validation (fresh postgres:16)

```
docker exec pg_validate2 psql -U chatnow_app -d chatnow -f /tmp/init-ledger.sql
```

Output: All CREATE TABLE / CREATE INDEX / CREATE TRIGGER / CREATE FUNCTION — no ERRORs.

Tables confirmed present:

- attribution_events ✅
- audit_events ✅ (extended with NOTIFICATION_SENT / NOTIFICATION_SUPPRESSED event types and template_id / consent_basis_id columns)
- identity_verification ✅
- ledger_entries ✅
- notification_consent_store ✅
- referral_links ✅
- studio_contracts ✅
- transactions ✅
- user_risk_profiles ✅

### 2. ESLint

```
./node_modules/.bin/eslint services/core-api/src/growth/ --ext .ts
```

Output: No errors. (TypeScript version compatibility warning is pre-existing and unrelated.)

## Work Completed

### WO-037: Creator-Led Attribution Engine

- **SQL**: Added `referral_links` table — tracks `creator_id`, `campaign_id`, `attribution_window_days`, `device_fingerprint`, `payment_instrument_hash` (hash only, no raw PAN). Append-only.
- **SQL**: Added `attribution_events` table — records every click/signup/conversion event with ledger link (`ledger_entry_id`, `rule_applied_id`) and `platform_time` (America/Toronto).
- **Service**: `ReferralRewardService` — every FIRST_PURCHASE/CONVERSION event generates a `REWARD_CREDIT` ledger entry with `wallet_bucket: 'promotional_bonus'` and `rule_applied_id`. Idempotency key prevents duplicate rewards.
- **Anti-fraud**: `assertNoSelfReferral()` blocks events where `device_fingerprint` or `payment_instrument_hash` matches the originating referral_link.

### WO-038: Consent-Aware Notification Service

- **SQL**: Added `notification_consent_store` table — fields: `user_id`, `channel` (Email/SMS/Push), `is_opted_in`, `jurisdiction_rule_version`, `updated_at`. Unique constraint on `(user_id, channel)`. Delete-blocked.
- **SQL**: Extended `audit_events.event_type` CHECK to include `NOTIFICATION_SENT` and `NOTIFICATION_SUPPRESSED`.
- **SQL**: Added `template_id` and `consent_basis_id` columns to `audit_events`.
- **Service**: `GuardedNotificationService` — checks `is_opted_in` before emitting; logs every attempt (sent or suppressed) to `audit_events` with `template_id` and `consent_basis_id`.

### Task 3: Platform Time Standardization

- All new attribution and notification log rows embed `platform_time` using `America/Toronto` via `GovernanceConfigService.TIMEZONE`.
- `attribution_events.platform_time` defaults to `NOW() AT TIME ZONE 'America/Toronto'` at the database level.

## Result

✅ SUCCESS — GROWTH_PRIMITIVES_SCAFFOLDED_PHASE_3_ACTIVE
