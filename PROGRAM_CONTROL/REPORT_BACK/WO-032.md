# WO-032 / FT-033 — REPORT BACK

## Branch + HEAD

- Branch: `copilot/add-hash-chaining-audit-service`
- HEAD: see commit pushed with this report

## Files Changed (`git diff --stat`)

```
docker-compose.yml                                               | 44 +++---
infra/postgres/init-ledger.sql                                   | 67 ++++++
services/core-api/src/config/governance.config.ts               | 17 ++++
services/core-api/src/finance/ledger.service.ts                  | 30 +++++
services/core-api/src/events/audit.service.ts                   | NEW
services/core-api/src/finance/types/ledger.types.ts             | NEW
4 files changed (plus 2 new files)
```

## Tasks Implemented

### TASK 1 — Cryptographic Hash-Chaining (Audit Service)

- **Created** `services/core-api/src/events/audit.service.ts`
  - `recordEvent()` computes `hash_current = SHA256(hash_prev + timestamp_utc + platform_time + actor_id + payload_hash)`
  - `update()` and `delete()` throw `DOCTRINE_VIOLATION_ERROR` unconditionally
- **Updated** `infra/postgres/init-ledger.sql` — appended `audit_events` table with `hash_prev CHAR(64)` and `hash_current CHAR(64) UNIQUE`, database-level append-only triggers (`trg_audit_events_no_update`, `trg_audit_events_no_delete`)

### TASK 2 — Finance Enum Standardization

- **Created** `services/core-api/src/finance/types/ledger.types.ts`
  - `WalletBucket` enum: `promotional_bonus`, `membership_allocation`, `purchased_tokens`
- **Updated** `services/core-api/src/finance/ledger.service.ts`
  - `recordEntry.amount` type narrowed from `number | bigint` → `bigint`
  - Runtime guard: throws `INVALID_AMOUNT` error if `typeof amount !== 'bigint'`

### TASK 3 — Dual-Timestamp Integration

- `AuditService.recordEvent()` populates `timestamp_utc` (ISO 8601 UTC) and `platform_time` (ISO 8601 `America/Toronto`) on every event
- Timezone resolved from `GovernanceConfigService.TIMEZONE`

### TASK 4 — Rule ID Inclusion

- `AuditService.recordEvent()` accepts optional `ruleAppliedId`; defaults to `GENERAL_GOVERNANCE_v10` and emits `REMEDIATION_REQUIRED` warning when omitted
- `LedgerService.recordEntry()` accepts optional `ruleAppliedId`; same default + warning behaviour; stored in entry `metadata.rule_applied_id`

### TASK 5 — Infrastructure Isolation (FT-033)

- **Rewrote** `docker-compose.yml` — removed all duplicate service blocks, removed host port bindings from `db` and `redis`, added explicit `backend` bridge network; only `api` service exposes port `3000:3000`

## Governance Config Fix

- **Fixed** `services/core-api/src/config/governance.config.ts` — the file had a dangling `public readonly TIMEZONE = ...` at module scope (invalid TypeScript). Converted to a proper `GovernanceConfigService` class exposing `TIMEZONE`, `PAYOUT_RATE_SHOWTHEATER`, and `PAYOUT_RATE_REGULAR`.

## Commands Run + Outputs

### ESLint (changed files only)

```
✅ 0 errors, 0 warnings on:
  services/core-api/src/events/audit.service.ts
  services/core-api/src/finance/types/ledger.types.ts
  services/core-api/src/finance/ledger.service.ts
  services/core-api/src/config/governance.config.ts
```

### SQL Validation (postgres:16)

```
✅ SQL applied successfully (all DDL: CREATE TABLE, CREATE FUNCTION, CREATE TRIGGER, CREATE INDEX, COMMENT)
```

### YAML Validation

```
✅ docker-compose.yml is valid YAML
```

## Result: ✅ SUCCESS
